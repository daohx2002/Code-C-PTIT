#include <stdio.h>

int main (){
	int t;
	scanf("%d", &t);
	while (t--){
	 int n;
	 scanf ("%d", &n);
	 int nt= 1;
	 for (int i = 2; i < n; i ++){
	 	if (n % i == 0){
	 		nt=0;
	 		break;
	 		if (nt=1)
			 print ("YES"\n);
			 else (nt=0)
			 print ("NO"\n);	
		 }
	 }
	 
}
